# MemDBG GitHub Pages guide

This folder contains the static user guide for MemDBG.

To deploy it with GitHub Pages, publish the contents of this folder as the site
root. If the repository uses GitHub Actions for Pages, set the upload path to
`github-pages/`.

